# SDK 54 pins react@19.1 while a web-only react-dom peer wants 19.2, so npm's
# strict peer resolution errors. This makes all installs (local, EAS Build, npm ci)
# use legacy peer resolution — the same flag used when setting the project up.
legacy-peer-deps=true
